# Journal — 2026-09-07

## 12:22 — Note

Set up `markdown.copyFiles.destination` (VS Code global + workspace settings) so pasted
images now land in a top-level `attachments/` folder instead of scattering as loose PNGs
next to whatever `.md` file they were pasted into. Migrated the one existing stray image
(`inbox/image-1.png` → `attachments/image-1.png`) and fixed its reference in
`inbox/to_be_triaged.md`.
