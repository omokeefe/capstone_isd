---
name: update-architecture
description: Make a change to the SysML/Cameo model, domain decomposition, or the XML architecture export, with traceability back to stakeholder needs or literature evidence. Use for Project_To-Do List.md §6-§10 architecture work.
---

Follow `assistant/workflows/update-architecture.md` exactly. Treat Cameo as the source
of truth and `nas_system_of_systems_architecture.xml` as a generated artifact; ensure
every new element traces to a stakeholder need (`assistant/memory/stakeholder-register.md`),
a source's evidence (`assistant/memory/source-register.md`), or the trajectory-intent
chain (`assistant/memory/project-brief.md`). Log real structural decisions in
`assistant/memory/decisions-log.md`, check off the relevant `Project_To-Do List.md` §10
boxes, and wrap up the session.
