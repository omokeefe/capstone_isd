# Archive

Inactive material, preserved but out of the active working surface — not deleted, just
moved here once something stops being current.

Empty as of the 2026-09-05 reorg: this workspace has exactly one active project and
nothing in it has become inactive yet. When something does (a superseded draft, a
completed and closed-out phase's working files), move it here with `git mv` rather than
deleting it, and note why in the commit or a one-line note alongside it.
